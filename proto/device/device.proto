syntax = "proto3";
package device;

import "common/common.proto";

message GetDeviceRequest {
  string dev_eui = 1;
}

service DeviceService {
  rpc Get (GetDeviceRequest) returns (common.KeyValue);
}